---
phase: 02-full-trackables-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/trackables.ts
  - src/lib/firestore.ts
  - src/components/IdentityPicker.tsx
autonomous: true
requirements:
  - TRACK-01
  - LOG-01
  - LOG-02

must_haves:
  truths:
    - "A getTrackables() async function exists and returns all 6 trackables in pills-first order"
    - "addLog() accepts a required loggedBy parameter and writes it to Firestore (no hardcoded 'caregiver')"
    - "IdentityPicker renders Me/Wife buttons with correct active/inactive styles and aria attributes"
  artifacts:
    - path: "src/lib/trackables.ts"
      provides: "Trackable interface + getTrackables() async function"
      exports: ["Trackable", "getTrackables"]
    - path: "src/lib/firestore.ts"
      provides: "Updated addLog() with loggedBy parameter"
      exports: ["logsCollection", "addLog"]
    - path: "src/components/IdentityPicker.tsx"
      provides: "Identity toggle UI component"
      exports: ["IdentityPicker"]
  key_links:
    - from: "src/components/IdentityPicker.tsx"
      to: "App.tsx"
      via: "onChange prop calling handleIdentityChange"
      pattern: "onChange.*Me.*Wife"
    - from: "src/lib/trackables.ts"
      to: "src/App.tsx"
      via: "getTrackables() call inside onAuthStateChanged callback"
      pattern: "getTrackables"
---

<objective>
Create the three foundational artifacts for Phase 2: the trackables config layer, the updated
Firestore write function, and the identity picker component. These are the contracts that
Plan 02 wires together in App.tsx and the component tree.

Purpose: Establish the abstracted config function, remove the hardcoded 'caregiver' loggedBy
value, and deliver the IdentityPicker UI component — all without touching App.tsx yet.
Output: src/lib/trackables.ts (new), src/lib/firestore.ts (updated), src/components/IdentityPicker.tsx (new)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-full-trackables-logging/02-CONTEXT.md
@.planning/phases/02-full-trackables-logging/02-UI-SPEC.md
@.planning/phases/02-full-trackables-logging/02-PATTERNS.md

<interfaces>
<!-- Current contracts the executor modifies or builds against. -->

From src/lib/firestore.ts (current — to be modified):
  interface LogEntry {
    trackableId: string
    timestamp: ReturnType<typeof serverTimestamp>
    loggedBy: string
    note?: string
  }
  export function logsCollection(subject: string): CollectionReference
  export async function addLog(subject: string, trackableId: string): Promise<void>
    // hardcoded loggedBy: 'caregiver' — remove this, add required param

From src/components/TrackableCard.tsx (read for button/aria pattern):
  // Button className pattern (template literal, no clsx):
  className={`bg-blue-600 text-white rounded-md px-4 py-2 min-h-[44px] font-semibold text-sm${writing ? ' opacity-50 cursor-not-allowed' : ''}`}
  // Named export only; no default export
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/lib/trackables.ts with Trackable interface and getTrackables()</name>
  <files>src/lib/trackables.ts</files>
  <read_first>
    - src/lib/firestore.ts — follow the interface-before-function and named-export patterns exactly
    - .planning/phases/02-full-trackables-logging/02-PATTERNS.md — Pattern Assignments section for trackables.ts (full target code excerpt)
    - .planning/phases/02-full-trackables-logging/02-CONTEXT.md — D-01 through D-06 for IDs, names, types, and function signature
  </read_first>
  <behavior>
    - getTrackables() returns exactly 6 items in this order: carprofen, gabapentin, trazodone (type:'pill'), then ate, peed, pooped (type:'activity')
    - Each item shape: { id: string, displayName: string, type: 'pill' | 'activity' }
    - Function is async and returns Promise<Trackable[]> so callers use await
    - Trackable interface is exported (consumed by TrackableList.tsx in Plan 02)
    - No external imports — file is self-contained
  </behavior>
  <action>
    Create src/lib/trackables.ts. Define and export the Trackable interface first (before the function — pattern from firestore.ts):
    `export interface Trackable { id: string; displayName: string; type: 'pill' | 'activity' }`
    Then export the async function:
    `export async function getTrackables(): Promise<Trackable[]>`
    Body is a static array literal returning the 6 trackables in D-01/D-02 order:
    carprofen (Carprofen, pill), gabapentin (Gabapentin, pill), trazodone (Trazodone, pill),
    ate (Ate, activity), peed (Peed, activity), pooped (Pooped, activity).
    No default export. Named exports only (per shared pattern in PATTERNS.md).
  </action>
  <verify>
    <automated>cd /Users/michael/Coding/madgy-ins-and-outs && npx tsc --noEmit 2>&1 | grep -i "trackables" || echo "no trackables errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/trackables.ts exists
    - `export interface Trackable` is present with id, displayName, type fields
    - `export async function getTrackables(): Promise<Trackable[]>` is present
    - Calling getTrackables() returns an array of length 6
    - First 3 items have type:'pill'; last 3 have type:'activity'
    - IDs are exactly: carprofen, gabapentin, trazodone, ate, peed, pooped
    - TypeScript compiles without errors in this file
    - No default export in the file
  </acceptance_criteria>
  <done>trackables.ts exists, exports Trackable interface and getTrackables(), TypeScript clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update src/lib/firestore.ts — add loggedBy param to addLog(), remove hardcoded value</name>
  <files>src/lib/firestore.ts</files>
  <read_first>
    - src/lib/firestore.ts — read in full before editing; the entire current file is shown in the interfaces block above
    - .planning/phases/02-full-trackables-logging/02-PATTERNS.md — firestore.ts section showing exact current and target signatures
    - .planning/phases/02-full-trackables-logging/02-CONTEXT.md — D-11
  </read_first>
  <behavior>
    - addLog() gains a required third parameter: loggedBy: string
    - The hardcoded string 'caregiver' inside the function body is removed
    - loggedBy parameter is used directly in the LogEntry object (shorthand property)
    - logsCollection() is unchanged
    - All imports are unchanged
    - LogEntry interface is unchanged
  </behavior>
  <action>
    Edit src/lib/firestore.ts. Change the addLog function signature from:
    `export async function addLog(subject: string, trackableId: string): Promise<void>`
    to:
    `export async function addLog(subject: string, trackableId: string, loggedBy: string): Promise<void>`
    Inside the function body, replace `loggedBy: 'caregiver'` with `loggedBy` (shorthand property —
    the parameter name matches the LogEntry field). No other changes to the file.
    Note: After this change, the existing App.tsx call `addLog('madgy', trackableId)` will become
    a TypeScript error — that is intentional and resolved in Plan 02 Task 2.
  </action>
  <verify>
    <automated>cd /Users/michael/Coding/madgy-ins-and-outs && grep -n "loggedBy" src/lib/firestore.ts</automated>
  </verify>
  <acceptance_criteria>
    - addLog signature has three parameters: subject: string, trackableId: string, loggedBy: string
    - The string literal 'caregiver' does not appear anywhere in firestore.ts
    - LogEntry object inside addLog uses `loggedBy` (shorthand, not `loggedBy: 'caregiver'`)
    - logsCollection() function is unchanged
    - File has no syntax errors (TypeScript parses cleanly in isolation)
  </acceptance_criteria>
  <done>addLog() updated with loggedBy parameter; hardcoded 'caregiver' removed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create src/components/IdentityPicker.tsx</name>
  <files>src/components/IdentityPicker.tsx</files>
  <read_first>
    - src/components/TrackableCard.tsx — button rendering, className template-literal pattern, aria-label, named export
    - .planning/phases/02-full-trackables-logging/02-PATTERNS.md — IdentityPicker.tsx section (full target component excerpt)
    - .planning/phases/02-full-trackables-logging/02-UI-SPEC.md — Section 4 (Color, picker button states), Section 5 (identity bar layout), Section 9 (Accessibility — aria-pressed, aria-label)
    - .planning/phases/02-full-trackables-logging/02-CONTEXT.md — D-07, D-08, D-09
  </read_first>
  <behavior>
    - Props: value: 'Me' | 'Wife' | null, onChange: (identity: 'Me' | 'Wife') => void
    - Renders a flex container: "I am:" label, then Me button, then Wife button
    - Container classes: mb-6 flex items-center gap-2
    - "I am:" label: text-sm font-semibold text-gray-600
    - Selected button (value matches): bg-blue-600 text-white rounded-md px-4 py-2 min-h-[44px] text-sm font-semibold
    - Unselected button: bg-white text-gray-700 border border-gray-300 rounded-md px-4 py-2 min-h-[44px] text-sm font-semibold
    - Each button has aria-label ("Set identity to Me" / "Set identity to Wife") and aria-pressed (true/false)
    - onClick calls onChange with the literal 'Me' or 'Wife'
    - No async, no error handling, no disabled state on picker buttons
    - Named export only; no default export
  </behavior>
  <action>
    Create src/components/IdentityPicker.tsx. Define the IdentityPickerProps interface first
    (before the component — pattern from TrackableCard.tsx):
    `interface IdentityPickerProps { value: 'Me' | 'Wife' | null; onChange: (identity: 'Me' | 'Wife') => void }`
    Define a local helper function buttonClass(isSelected: boolean): string that returns the full
    Tailwind class string using a template literal (no clsx — not in codebase):
    base = 'rounded-md px-4 py-2 min-h-[44px] text-sm font-semibold'
    selected suffix = ' bg-blue-600 text-white'
    unselected suffix = ' bg-white text-gray-700 border border-gray-300'
    Export the IdentityPicker function (named export). Render a div with className "mb-6 flex items-center gap-2".
    Inside: a span for "I am:" label, then two buttons. Each button calls onChange with the
    literal value ('Me' or 'Wife'). Set aria-label to "Set identity to Me" / "Set identity to Wife".
    Set aria-pressed to {value === 'Me'} / {value === 'Wife'} (boolean — React renders as "true"/"false").
    No imports needed (React is available via JSX transform).
  </action>
  <verify>
    <automated>cd /Users/michael/Coding/madgy-ins-and-outs && npx tsc --noEmit 2>&1 | grep -i "identity\|picker" || echo "no IdentityPicker errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/IdentityPicker.tsx exists
    - IdentityPickerProps interface defined with value and onChange
    - Export is named (export function IdentityPicker) — no default export
    - Container div has classes: mb-6 flex items-center gap-2
    - "I am:" label rendered in a span with text-sm font-semibold text-gray-600
    - Me button has aria-label="Set identity to Me" and aria-pressed={value === 'Me'}
    - Wife button has aria-label="Set identity to Wife" and aria-pressed={value === 'Wife'}
    - Selected button class string contains bg-blue-600 and text-white
    - Unselected button class string contains bg-white, text-gray-700, border, border-gray-300
    - Both buttons have min-h-[44px] in className
    - TypeScript compiles without errors in this file
  </acceptance_criteria>
  <done>IdentityPicker.tsx exists with correct props, aria attributes, and visual states</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → React state | Value read from localStorage on mount is cast to a union type — must be validated before use |
| User tap → Firestore write | loggedBy value originates from identity state (from localStorage or button tap) — no server validation in MVP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | localStorage `madgy_caregiver` | accept | Private family tool on personal devices; value only affects display attribution, not access control; Firestore rules enforce auth, not loggedBy value |
| T-02-02 | Spoofing | addLog loggedBy param | accept | No identity verification in MVP; anonymous auth gates writes but does not validate loggedBy string; acceptable for two-person household tool |
| T-02-03 | Information Disclosure | localStorage key `madgy_caregiver` | accept | Contains only "Me" or "Wife" — no PII, no secrets |
| T-02-SC | Tampering | npm installs | accept | No new npm packages in this plan — static array in trackables.ts, no new dependencies |
</threat_model>

<verification>
After all three tasks complete:
1. `npx tsc --noEmit` from project root — expect errors ONLY on App.tsx (addLog call missing third arg); trackables.ts and IdentityPicker.tsx should be clean
2. `grep -n "caregiver" src/lib/firestore.ts` — should return no matches
3. `grep -n "export" src/lib/trackables.ts` — should show both `export interface Trackable` and `export async function getTrackables`
4. `grep -n "export" src/components/IdentityPicker.tsx` — should show `export function IdentityPicker`
</verification>

<success_criteria>
- src/lib/trackables.ts exists; exports Trackable interface and getTrackables() returning 6 items pills-first
- src/lib/firestore.ts updated; addLog() has required loggedBy: string param; no hardcoded 'caregiver' string
- src/components/IdentityPicker.tsx exists; renders Me/Wife buttons with correct Tailwind classes and aria attributes
- TypeScript compiles cleanly in all three files (App.tsx errors from the updated addLog signature are expected and will be fixed in Plan 02)
</success_criteria>

<output>
Create `.planning/phases/02-full-trackables-logging/02-01-SUMMARY.md` when done
</output>
