---
phase: 03-display-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/elapsed.ts
  - src/App.tsx
autonomous: true
requirements:
  - DISP-01

must_haves:
  truths:
    - "formatElapsed(undefined) returns 'Never'"
    - "formatElapsed(timestamp < 60s ago) returns 'just now'"
    - "formatElapsed(timestamp >= 60s ago) returns 'Nh Nm ago' with both units always present"
    - "App.tsx snapshot callback reduces docs into Record<string, Timestamp> and stores via setLastLogged"
    - "App.tsx setInterval fires every 30s and increments tick state to force re-render"
    - "lastLogged state is passed as prop to TrackableList"
  artifacts:
    - path: "src/lib/elapsed.ts"
      provides: "formatElapsed pure utility function"
      exports: ["formatElapsed"]
    - path: "src/App.tsx"
      provides: "lastLogged state, snapshot callback, setInterval tick"
      contains: "setLastLogged"
  key_links:
    - from: "src/App.tsx"
      to: "src/lib/elapsed.ts"
      via: "import { formatElapsed } from './lib/elapsed'"
      pattern: "formatElapsed"
    - from: "onSnapshot callback"
      to: "lastLogged state"
      via: "setLastLogged(map)"
      pattern: "setLastLogged"
---

<objective>
Create the elapsed-time utility and wire the Firestore snapshot callback + 30-second interval timer in App.tsx.

Purpose: Establish the data pipeline that converts raw Firestore log timestamps into a `Record<string, Timestamp>` map stored in React state, and trigger re-renders on a 30-second cadence so elapsed text stays current without a page refresh.

Output: `src/lib/elapsed.ts` (pure formatter), updated `src/App.tsx` (snapshot callback filled, setInterval added, lastLogged state threaded to TrackableList).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-display-polish/03-CONTEXT.md
@.planning/phases/03-display-polish/03-UI-SPEC.md
@.planning/phases/03-display-polish/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/lib/elapsed.ts — formatElapsed pure utility</name>
  <read_first>
    src/lib/trackables.ts (pattern: pure export, typed interface, no side effects)
    .planning/phases/03-display-polish/03-CONTEXT.md (D-01 through D-08 — format rules)
    .planning/phases/03-display-polish/03-UI-SPEC.md (§6 — format table and computation spec)
    .planning/phases/03-display-polish/03-PATTERNS.md (src/lib/elapsed.ts section — target file structure)
  </read_first>
  <behavior>
    - formatElapsed(undefined) → 'Never' (capital N, word only)
    - formatElapsed(Timestamp where Date.now() - ts.toMillis() < 60_000) → 'just now' (all lowercase)
    - formatElapsed(Timestamp where elapsed = 2700000ms, i.e. 45min) → '0h 45m ago'
    - formatElapsed(Timestamp where elapsed = 9000000ms, i.e. 2h 30min) → '2h 30m ago'
    - formatElapsed(Timestamp where elapsed = 50400000ms, i.e. 14h exactly) → '14h 0m ago'
    - formatElapsed(Timestamp where elapsed = 97200000ms, i.e. 27h) → '27h 0m ago' (no day units)
  </behavior>
  <action>
    Create src/lib/elapsed.ts. The file has one export: `formatElapsed(timestamp: Timestamp | undefined): string`. Import `Timestamp` from `'firebase/firestore'`.

    Logic:
    - If timestamp is undefined, return `'Never'`.
    - Compute `elapsedMs = Date.now() - timestamp.toMillis()`.
    - If elapsedMs < 60_000, return `'just now'`.
    - Otherwise: `totalMinutes = Math.floor(elapsedMs / 60_000)`, `hours = Math.floor(totalMinutes / 60)`, `minutes = totalMinutes % 60`. Return template literal `` `${hours}h ${minutes}m ago` ``.

    No day conversion. No special-casing zero hours or zero minutes (per D-01 and D-03). No React imports, no side effects.

    For TDD: create `src/lib/elapsed.test.ts` using vitest. Use `vi.setSystemTime` to control `Date.now()`. Import a real `Timestamp` from `firebase/firestore` (use `Timestamp.fromMillis(ms)` to construct test values). Test each branch: undefined input, sub-60s elapsed, exact boundary at 60s, 45min, 2h30m, 14h0m, 27h.
  </action>
  <verify>
    <automated>cd /Users/michael/Coding/madgy-ins-and-outs && npx vitest run src/lib/elapsed.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All formatElapsed test cases pass. src/lib/elapsed.ts exports only formatElapsed. npx tsc -p tsconfig.app.json --noEmit reports 0 errors for elapsed.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx — snapshot callback, setInterval tick, lastLogged state</name>
  <read_first>
    src/App.tsx (current full file — extend, do not rewrite; snapshot callback at line 38 is the target)
    src/lib/elapsed.ts (just created — import formatElapsed and Timestamp from it)
    .planning/phases/03-display-polish/03-PATTERNS.md (App.tsx section — exact state additions, snapshot callback body, setInterval pattern, prop threading)
    .planning/phases/03-display-polish/03-CONTEXT.md (D-05, D-06, D-07 — snapshot reduction logic, latest-wins rule, tick trigger)
    .planning/phases/03-display-polish/03-UI-SPEC.md (§6 Computation, §6 Auto-update)
  </read_first>
  <action>
    Extend src/App.tsx with four targeted changes. Do not restructure the file or change unrelated code.

    1. Add imports at the top: `import { Timestamp } from 'firebase/firestore'` and `import { formatElapsed } from './lib/elapsed'`. The `formatElapsed` import is used in Plan 02 (TrackableList calls it), but Timestamp is needed for the state type here.

    2. Add two state declarations alongside the existing four (after the identity useState line):
       - `const [lastLogged, setLastLogged] = useState<Record<string, Timestamp>>({})` — holds the latest timestamp per trackableId.
       - `const [, setTick] = useState(0)` — tick counter; incrementing it forces a re-render so elapsed strings recompute.

    3. Replace the empty onSnapshot callback (line 38: `() => {` with comment) with the reduction body. The callback receives a `snapshot` parameter. Inside: declare `const map: Record<string, Timestamp> = {}`. Call `snapshot.docs.forEach((doc) => { ... })`. Inside forEach: `const data = doc.data()`, `const id: string = data.trackableId`, `const ts: Timestamp = data.timestamp`. Condition: `if (!map[id] || ts.toMillis() > map[id].toMillis()) { map[id] = ts }`. After forEach: `setLastLogged(map)`. This implements D-05 and D-06 (latest timestamp per trackableId, all caregivers count).

    4. Add a new useEffect after the existing onSnapshot useEffect (around line 44). It sets up the 30-second timer: `const id = setInterval(() => setTick((n) => n + 1), 30000)` and returns `() => clearInterval(id)`. Dependency array is `[]` (runs once on mount). Per D-04.

    5. Thread `lastLogged` as a prop on the TrackableList JSX element (around line 72–75): add `lastLogged={lastLogged}` alongside the existing trackables, onLog, and disabled props.

    Note: TrackableList does not yet accept a `lastLogged` prop — that is added in Plan 02. TypeScript will error on this prop until Plan 02 runs. The build will remain broken until both plans complete. This is expected and acceptable for sequential wave execution.
  </action>
  <verify>
    <automated>cd /Users/michael/Coding/madgy-ins-and-outs && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -v "lastLogged" | grep "error TS" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `const [lastLogged, setLastLogged] = useState<Record<string, Timestamp>>({})` (source assertion)
    - src/App.tsx contains `const [, setTick] = useState(0)` (source assertion)
    - The onSnapshot callback body contains `setLastLogged(map)` — not the Phase 1 comment (source assertion: `grep -c "setLastLogged" src/App.tsx` outputs 2 — one declaration, one call)
    - src/App.tsx contains `setInterval` with `30000` as the interval argument (source assertion)
    - `<TrackableList` JSX in App.tsx includes `lastLogged={lastLogged}` (source assertion)
    - TypeScript errors unrelated to the Plan-02 TrackableList prop are 0 (verified via the automated command above filtering out "lastLogged" errors)
  </acceptance_criteria>
  <done>App.tsx has lastLogged state, the snapshot callback reduces docs into the map, the setInterval tick is wired, and lastLogged is threaded to TrackableList. The only TypeScript errors remaining are the expected "Property 'lastLogged' does not exist" on TrackableList (resolved in Plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → App.tsx | Snapshot docs arrive from Firestore; data.trackableId and data.timestamp are read without validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | App.tsx onSnapshot — `data.trackableId` cast to string | accept | trackableId is a string key used only for local Map lookup; worst case: unknown key in map → formatElapsed(undefined) → "Never" display; no write path affected |
| T-03-02 | Tampering | App.tsx onSnapshot — `data.timestamp` cast to Timestamp | accept | invalid timestamp would cause toMillis() to throw; caught by existing error boundary or produces "Never"; no PII at risk |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | no new packages installed in this plan; vitest is already in devDependencies |
</threat_model>

<verification>
After both tasks complete:
- `npx vitest run src/lib/elapsed.test.ts` — all tests pass
- `npx tsc -p tsconfig.app.json --noEmit` — only expected TrackableList prop error; 0 other errors
- `grep -c "setLastLogged" src/App.tsx` outputs 2
- `grep "setInterval" src/App.tsx` matches a line with 30000
</verification>

<success_criteria>
- formatElapsed handles all three cases correctly (Never / just now / Nh Nm ago) with tests proving each branch
- App.tsx snapshot callback is populated and reduces to a lastLogged map
- 30-second tick interval is registered and cleaned up on unmount
- lastLogged prop is threaded to TrackableList (even if TypeScript protests until Plan 02)
</success_criteria>

<output>
Create `.planning/phases/03-display-polish/03-01-SUMMARY.md` when done.
</output>
