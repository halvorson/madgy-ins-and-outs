---
phase: 03-display-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/TrackableList.tsx
  - src/components/TrackableCard.tsx
autonomous: false
requirements:
  - DISP-01
  - DISP-03

must_haves:
  truths:
    - "Each trackable card shows a computed elapsed string — 'just now', 'Nh Nm ago', or 'Never' — not an em-dash"
    - "Tapping Log on a card immediately shows 'just now' on that card (snapshot fires before next tick)"
    - "The trackable list is split into a Pills section and an Activities section, each with a section header"
    - "Within each section, cards appear in config order (Carprofen → Gabapentin → Trazodone; Ate → Peed → Pooped)"
    - "Section headers use text-xs uppercase tracking-wide text-gray-400 styling"
    - "Elapsed text uses text-sm font-normal leading-relaxed text-gray-500 styling"
    - "The full list is scrollable on a 375px screen with no horizontal overflow"
  artifacts:
    - path: "src/components/TrackableList.tsx"
      provides: "Grouped rendering with Pills and Activities sections"
      contains: "t.type === 'pill'"
    - path: "src/components/TrackableCard.tsx"
      provides: "lastLoggedText prop rendered in elapsed slot"
      contains: "lastLoggedText"
  key_links:
    - from: "src/components/TrackableList.tsx"
      to: "src/lib/elapsed.ts"
      via: "import { formatElapsed } from '../lib/elapsed'"
      pattern: "formatElapsed"
    - from: "src/components/TrackableList.tsx"
      to: "src/components/TrackableCard.tsx"
      via: "lastLoggedText={formatElapsed(lastLogged[t.id])}"
      pattern: "lastLoggedText"
---

<objective>
Update TrackableList and TrackableCard to consume the lastLogged map from App.tsx and render grouped sections with live elapsed-time text. Then verify end-to-end on device.

Purpose: Complete the DISP-01 and DISP-03 display layer. After this plan, the app shows elapsed time per trackable, groups cards into Pills and Activities sections, and is verified to work on a mobile screen.

Output: Updated TrackableList.tsx (grouped rendering, formatElapsed call, lastLogged prop), updated TrackableCard.tsx (lastLoggedText prop replaces &mdash;), human checkpoint confirming visual and functional correctness.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-display-polish/03-CONTEXT.md
@.planning/phases/03-display-polish/03-UI-SPEC.md
@.planning/phases/03-display-polish/03-PATTERNS.md
@.planning/phases/03-display-polish/03-01-SUMMARY.md

<interfaces>
From src/lib/elapsed.ts (created in Plan 03-01):
  export function formatElapsed(timestamp: Timestamp | undefined): string
  // Returns: 'Never' | 'just now' | 'Nh Nm ago'

From src/lib/trackables.ts:
  export interface Trackable {
    id: string
    displayName: string
    type: 'pill' | 'activity'
  }

From src/App.tsx (updated in Plan 03-01):
  // lastLogged state type
  Record<string, Timestamp>
  // threaded to TrackableList as: lastLogged={lastLogged}

Current src/components/TrackableList.tsx props interface:
  interface TrackableListProps {
    trackables: Trackable[]
    onLog: (trackableId: string) => Promise<void>
    disabled?: boolean
  }

Current src/components/TrackableCard.tsx props interface:
  interface TrackableCardProps {
    id: string
    displayName: string
    onLog: () => Promise<void>
    disabled?: boolean
  }
  // Line 32-34: <p className="text-sm font-normal leading-relaxed text-gray-500 mt-1">&mdash;</p>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TrackableCard.tsx — add lastLoggedText prop, replace &mdash; slot</name>
  <read_first>
    src/components/TrackableCard.tsx (full file — current props interface at lines 3–8, elapsed slot at lines 32–34)
    .planning/phases/03-display-polish/03-UI-SPEC.md (§7.1 TrackableCard — exact prop name, class, and element spec)
    .planning/phases/03-display-polish/03-PATTERNS.md (TrackableCard section — before/after for props interface and elapsed slot)
  </read_first>
  <action>
    Make two targeted edits to src/components/TrackableCard.tsx. Do not touch the error handling, button logic, or any other JSX.

    1. Add `lastLoggedText: string` to the `TrackableCardProps` interface (after the `disabled?: boolean` line).

    2. Add `lastLoggedText` to the destructure on the export function line: `export function TrackableCard({ displayName, onLog, disabled, lastLoggedText }: TrackableCardProps)`.

    3. In the JSX, replace the `&mdash;` literal with `{lastLoggedText}`. The `<p>` element class stays exactly as-is: `className="text-sm font-normal leading-relaxed text-gray-500 mt-1"`. Per UI-SPEC §7.1: the card receives a pre-computed string and renders it verbatim — no formatting logic inside the card.
  </action>
  <verify>
    <automated>cd /Users/michael/Coding/madgy-ins-and-outs && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep "TrackableCard" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "lastLoggedText" src/components/TrackableCard.tsx` matches at least 3 lines: interface field, destructure, JSX render
    - `grep "mdash" src/components/TrackableCard.tsx` returns no output (em-dash removed)
    - `grep "lastLoggedText: string" src/components/TrackableCard.tsx` returns a match (prop is typed string, not optional)
    - `npx tsc -p tsconfig.app.json --noEmit` reports 0 errors for TrackableCard.tsx
  </acceptance_criteria>
  <done>TrackableCard accepts and renders lastLoggedText. The &mdash; placeholder is gone. TypeScript is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Update TrackableList.tsx — add lastLogged prop, grouped section rendering</name>
  <read_first>
    src/components/TrackableList.tsx (full file — current flat ul rendering at lines 10–25)
    src/lib/elapsed.ts (confirms formatElapsed export signature)
    .planning/phases/03-display-polish/03-UI-SPEC.md (§5 Layout — section structure, §7.1 TrackableList — exact h2 classes and ul structure)
    .planning/phases/03-display-polish/03-PATTERNS.md (TrackableList section — complete renderGroup pattern)
    .planning/phases/03-display-polish/03-CONTEXT.md (D-09, D-10, D-11 — grouping rules, section labels, order preservation)
  </read_first>
  <action>
    Replace TrackableList.tsx with the grouped implementation. The file grows from ~25 lines to ~45 lines. Preserve all existing imports and extend them.

    Updated imports:
    - Keep: `import { Trackable } from '../lib/trackables'` and `import { TrackableCard } from './TrackableCard'`
    - Add: `import { Timestamp } from 'firebase/firestore'` and `import { formatElapsed } from '../lib/elapsed'`

    Updated props interface — add `lastLogged: Record<string, Timestamp>` (required, not optional):
      trackables: Trackable[]
      onLog: (trackableId: string) => Promise<void>
      disabled?: boolean
      lastLogged: Record<string, Timestamp>

    Destructure: `{ trackables, onLog, disabled, lastLogged }`.

    Component body — replace the flat `<ul>` with grouped rendering:
    - `const pills = trackables.filter((t) => t.type === 'pill')`
    - `const activities = trackables.filter((t) => t.type === 'activity')`
    - Define a `renderGroup(items: Trackable[], label: string, addTopMargin: boolean)` helper that returns a `<div>` with `className={addTopMargin ? 'mt-6' : ''}`. Inside: `<h2 className="text-xs font-normal uppercase tracking-wide text-gray-400 mb-2">{label}</h2>` followed by `<ul className="flex flex-col gap-6">`. Each item in the ul: `<li key={t.id}><TrackableCard id={t.id} displayName={t.displayName} onLog={() => onLog(t.id)} disabled={disabled} lastLoggedText={formatElapsed(lastLogged[t.id])} /></li>`.
    - Return `<div>{renderGroup(pills, 'Pills', false)}{renderGroup(activities, 'Activities', true)}</div>`.

    Section label copy (per D-09 and UI-SPEC §9): `'Pills'` (capital P) for type `'pill'`, `'Activities'` (capital A) for type `'activity'`. These are rendered as-is — the h2 class applies `uppercase` so they display as PILLS / ACTIVITIES, but the source strings use title case.

    Order within each group is preserved from filter order (which preserves config order — per D-10). No sorting.

    formatElapsed is called at the TrackableList boundary: `formatElapsed(lastLogged[t.id])`. When `t.id` is absent from the map, `lastLogged[t.id]` is `undefined`, which formatElapsed maps to `'Never'` — the correct never-logged state per D-08.
  </action>
  <verify>
    <automated>cd /Users/michael/Coding/madgy-ins-and-outs && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep "error TS" | wc -l | tr -d ' ' && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "t.type === 'pill'" src/components/TrackableList.tsx` returns a match (grouping logic present)
    - `grep "formatElapsed" src/components/TrackableList.tsx` returns a match (formatElapsed called at list boundary)
    - `grep "lastLoggedText" src/components/TrackableList.tsx` returns a match (prop passed to TrackableCard)
    - `grep "text-xs.*uppercase.*tracking-wide.*text-gray-400" src/components/TrackableList.tsx` returns a match (section header class)
    - `grep "mt-6" src/components/TrackableList.tsx` returns a match (Activities section top margin)
    - `npx tsc -p tsconfig.app.json --noEmit` reports 0 errors
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>TrackableList renders two grouped sections (Pills, Activities) with correct section headers. formatElapsed is called per card. TypeScript and build both pass clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 3 display layer is wired: elapsed-time text computed from Firestore snapshot data, grouped Pills/Activities sections, 30-second auto-refresh, never-logged "Never" state.

    To deploy and test: run `npm run build && firebase deploy --only hosting` from the project root. Or run `npm run dev` and open the browser at localhost:5173 to test locally.
  </what-built>
  <how-to-verify>
    Open the app on your phone (or browser at 375px viewport width). Check all four Phase 3 success criteria from ROADMAP.md:

    1. ELAPSED TIME: Each trackable card shows an elapsed string below the name. A card with a recent log shows "Xh Ym ago" with both units (e.g. "0h 12m ago"). A card with no log shows "Never". If you just logged something, it shows "just now".

    2. JUST NOW: Tap Log on any card. The elapsed text on that card should immediately change to "just now" — not wait 30 seconds. (The snapshot fires before the next tick.)

    3. GROUPING: The list is split into two sections. "PILLS" header appears above Carprofen, Gabapentin, Trazodone. "ACTIVITIES" header appears above Ate, Peed, Pooped. Both headers are small, uppercase, muted gray.

    4. MOBILE LAYOUT: Resize your browser to 375px wide (or test on your phone). The full list should be readable without horizontal scrolling. All Log buttons should be easily tappable one-handed.

    5. AUTO-UPDATE: Wait 30 seconds after a log. The elapsed time should tick forward from "just now" to "0h 0m ago" (or remain "just now" if still under 60s). After 1 minute it should show "0h 1m ago".
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe which check failed and what you saw instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.tsx → TrackableList → TrackableCard | lastLogged map flows down as props; formatElapsed output is a plain string rendered as text content (not innerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | formatElapsed output in DOM | accept | elapsed text reveals when a log was made, which is intentional product behavior; no PII other than timing |
| T-03-04 | Denial of Service | setInterval 30s tick in App.tsx | accept | single interval, cleaned up on unmount via clearInterval; negligible resource use |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | no new packages installed in this plan |
</threat_model>

<verification>
After all tasks complete:
- `npx tsc -p tsconfig.app.json --noEmit` — 0 errors
- `npm run build` — exits 0, no warnings about elapsed or TrackableList
- Human checkpoint approved: all five verification items pass
- `grep -r "mdash" src/` returns no matches (em-dash fully removed)
- `grep "Never" src/lib/elapsed.ts` returns a match (Never-logged case handled in utility, not in components)
</verification>

<success_criteria>
- Every trackable shows "just now", "Nh Nm ago", or "Never" — never an em-dash
- Tapping Log immediately shows "just now" on that card without waiting for the 30s tick
- Two sections visible: PILLS (Carprofen, Gabapentin, Trazodone) and ACTIVITIES (Ate, Peed, Pooped)
- App renders correctly and is scrollable at 375px without horizontal overflow
- TypeScript compiles clean, production build succeeds
- Human checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/03-display-polish/03-02-SUMMARY.md` when done.
</output>
